# ArangoDB driver
python-arango>=7.5

# Scientific stack (use case 1/2/3 + build_omics_collections)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3        # (controlla se usato; alcuni UC fanno enrichment)

# Plotting
matplotlib>=3.7
seaborn>=0.12
plotly>=5.17             # usato da load_graph_to_arangodb + plots.py

# Graph
networkx>=3.1

# Progress
tqdm>=4.65

# RDF / PKT (solo per build_property_graph + kg_utils)
rdflib>=7.0

# Util
requests>=2.31

# Benchmarks (scripts/benchmark/)
pyarrow>=14.0            # Parquet baseline in bench_schema.py
psutil>=5.9              # machine specs recorded in environment.json

#Optional: for mapping
pybiomart>=0.2.0
sqlalchemy>=2.0
mygene>=3.2