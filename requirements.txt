# ArangoDB driver
python-arango>=7.5

# Scientific stack (use cases 1/2/3 + build_omics_collections)
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Plotting
matplotlib>=3.7
seaborn>=0.12
plotly>=5.17             # interactive subgraph rendering in plots.py

# Graph
networkx>=3.1

# Progress
tqdm>=4.65

# Util
requests>=2.31

# Benchmarks (scripts/benchmark/)
pyarrow>=14.0            # Parquet baseline in bench_schema.py
psutil>=5.9              # machine specs recorded in environment.json

# Optional — only for `db_dump.py download`
huggingface_hub>=0.20

# Optional — provenance scripts under scripts/mapping/, not part of the
# pipeline. Install only to regenerate the shipped mapping tables.
pybiomart>=0.2.0
sqlalchemy>=2.0
biopython>=1.81          # NCBI Entrez, biomart_extender.py --no-skip-ncbi
